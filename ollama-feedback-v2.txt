### **HIGHEST-IMPACT NEXT STEPS**  
*(Prioritized for polish, UX, and content completeness within constraints)*  

---

#### **1. Fix Hardcoded Subtitle & Global Direction Consistency**  
- **Action**: Replace the hardcoded "Master Italian vocabulary" subtitle with a dynamic value that updates based on the global direction toggle (e.g., "Master [Target Language] Vocabulary").  
- **Why**: A polished app must feel dynamic and responsive to user choices. This small fix signals attention to detail and consistency.  
- **Implementation**: Use JavaScript to update the subtitle in real-time when the direction toggle is activated.  

---

#### **2. Sync PatternsTab Direction Selector with Global Toggle**  
- **Action**: Ensure the PatternsTab’s direction selector is **linked to the global toggle** (not isolated).  
- **Why**: Inconsistent controls break UX flow. Users should never see conflicting direction settings.  
- **Implementation**: Use a shared state variable (e.g., `directionState`) in IndexedDB or a global JS variable to synchronize both controls.  

---

#### **3. Per-Direction SRS & Progress Tracking**  
- **Action**: Store SRS intervals and progress **separately for EN→IT and IT→EN** in IndexedDB.  
- **Why**: Without this, the app feels unpolished and ineffective for bidirectional learning. Users need to see clear progress in both directions.  
- **Implementation**:  
  - Add `direction` metadata to SRS cards (e.g., `card.direction: "en-it"`).  
  - Create separate progress counters for each direction (e.g., `progress.en-it`, `progress.it-en`).  

---

#### **4. Optimize Bundle Size (1.2MB → Target 800KB)**  
- **Action**: Use Vite’s code-splitting and tree-shaking to reduce unused code.  
- **Why**: A smaller bundle improves load times and perceived polish.  
- **Implementation**:  
  - Lazy-load non-critical components (e.g., audio, advanced exercises).  
  - Compress ComfyUI-generated images to WebP format.  
  - Remove redundant polyfills or libraries.  

---

#### **5. Polish Content Audit System with Local LLM**  
- **Action**: Add a **visual audit dashboard** (e.g., a "Content Health" tab) that shows:  
  - Grammar tagging accuracy.  
  - Overused vocabulary.  
  - Cultural fluency rule adherence.  
- **Why**: This demonstrates depth and quality control, making the app feel professional.  
- **Implementation**: Use Ollama to generate Markdown reports and render them in a dedicated UI tab.  

---

#### **6. Polish Fill-in-the-Blank & Branching Dialogue UX**  
- **Action**: Add **real-time feedback** for fill-in-the-blank exercises (e.g., highlight correct/incorrect answers) and **clear navigation** in branching dialogues.  
- **Why**: These features are critical for language learning and must feel intuitive.  
- **Implementation**:  
  - Use CSS animations for feedback (e.g., green checkmarks, red crosses).  
  - Add a "Back" button to branching dialogues to prevent user frustration.  

---

#### **7. Polish Cultural Fluency Integration**  
- **Action**: Embed **cultural fluency rules into lessons** (e.g., pop-up tips in dialogues, "Did You Know?" sections).  
- **Why**: This adds depth and makes the app feel like a holistic learning tool,